# Python
__pycache__/
*.pyc
*.pyo

# 产物 (跑 build_index.py / gen_announce.py 重新生成)
index.html

# 截图和进度 (含真实转账凭证, 不入库)
screenshots/_inbox/
screenshots/by_date/
screenshots/manual_screens/
screenshots/capture_progress.json

# 公告原文件 (含真实社区 + 短地址 + 业绩, 不入库)
达标公告.txt

# Telegram 凭据 (绝对不能推)
.env
tg_session*.txt

# serve.py 日志
serve_err.log
serve_out.log

# watchdog 锁文件
.watchdog.lock

# whitelist_bot.py 运行产物 (log + 去重表, 持续增长)
whitelist_bot.log
whitelist_processed.json

# whitelist_bot_v2.py 运行产物 (V2 独立, 跟 V1 分离)
whitelist_v2.log
whitelist_v2_processed.json

# 群组映射表 备份 (历史数据, 不入库)
群组映射表.json.bak_*
群组映射表.json.prev

# 临时 / 调试
_*.log
_*.py
*_v2.log

# 系统
.DS_Store
Thumbs.db
desktop.ini
